<UserControl x:Class="Progent.DockpaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:local="clr-namespace:Progent"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="300"
             d:DataContext="{Binding Path=local.DockpaneViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="LogEntryStyle" TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Margin" Value="0,2"/>
            </Style>

            <DataTemplate x:Key="LogDataTemplate" DataType="{x:Type local:LogEntry}">
                <Border BorderBrush="{DynamicResource Esri_Color_Gray155}" BorderThickness="0,0,0,1" Padding="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <TextBlock Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}" FontWeight="Bold" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Message}" FontWeight="Bold"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1" Text="{Binding Details}" TextWrapping="Wrap" Margin="0,5,0,0" FontStyle="Italic"
                                   Visibility="{Binding Details, Converter={StaticResource NullOrEmptyToVisibilityConverter}}"/>
                    </Grid>
                </Border>
                <DataTemplate.Triggers>
                    <DataTrigger Binding="{Binding Type}" Value="Error">
                        <Setter Property="Foreground" Value="{DynamicResource Esri_Color_Red}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Type}" Value="FunctionCall">
                        <Setter Property="Foreground" Value="{DynamicResource Esri_Color_Blue}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Type}" Value="FunctionResult">
                        <Setter Property="Foreground" Value="{DynamicResource Esri_Color_Green}"/>
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>

            <local:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter"/>

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{DynamicResource Esri_CardStyle}" Padding="10" Margin="5">
            <StackPanel>
                <TextBlock Text="Server URL" Style="{DynamicResource Esri_TextBlockH5}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding ServerUrl}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Button Grid.Row="1" Content="{Binding ConnectButtonText}" Command="{Binding ConnectCommand}" Margin="5,0,5,5" Style="{DynamicResource Esri_ButtonFill}"/>

        <ListView Grid.Row="2" ItemsSource="{Binding LogEntries}"
                  ItemTemplate="{StaticResource LogDataTemplate}"
                  ItemContainerStyle="{StaticResource LogEntryStyle}"
                  BorderThickness="0"
                  HorizontalContentAlignment="Stretch"/>

    </Grid>
</UserControl>

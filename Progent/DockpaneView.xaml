<UserControl x:Class="Progent.DockpaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Server URL:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{DynamicResource Esri_TextBlock}"/>
            <TextBox Text="{Binding ServerUrl}" VerticalAlignment="Center" Style="{DynamicResource Esri_TextBox}"/>
        </DockPanel>

        <Button Grid.Row="1" Content="{Binding ConnectButtonText}" Command="{Binding ConnectCommand}" Margin="10,0,10,10" Style="{DynamicResource Esri_Button}"/>

        <TextBox Grid.Row="2" Text="{Binding LogText}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Margin="10,0,10,10" Style="{DynamicResource Esri_TextBox}"/>

    </Grid>
</UserControl>
